# 验证记录

## 已完成

- 修改仅涉及博客 Category/index.vue 的封面布局和交互样式。
- 桌面等宽两列，封面独立 16:10；900px 及以下单列。
- 主图 contain 居中完整适配，删除推荐位和列表主图悬停缩放。
- `npm run build` 通过：Vite 构建、201 个 gzip 文件校验及性能预算检查均成功。
- 初始资源 gzip：JS 127.5KB，CSS 9.9KB。
- `git diff --check` 通过；OpenSpec 严格校验通过。
- 构建存在 Sass 弃用、NODE_ENV 配置及大资源块提示，未导致失败。

## 尚未完成

浏览器工具在读取可用浏览器时连接失败，无法取得浏览器会话。尚未执行 1920/1280/901/900/390px 页面实测、截图、非标准图片与长短内容检查、悬停及键盘导航验收。任务 2.2–2.5 保留未完成，不能以源码检查替代视觉验收。

## 发布范围

用户随后明确要求部署，已于 2026-09-15 发布博客前端，版本 cover-display-20260915-135246。未修改后台、数据库或 Nginx 配置。线上入口文件 SHA256 与本地构建一致，分类页新 CSS 返回 200，固定比例、两列布局和取消缩放规则已确认。/blog、/blog/tdhz 与 /api/websiteInfo/front 请求成功。浏览器视觉验收仍未完成。服务器保留该版本发布前的静态文件以供还原。如需还原，应仅还原本次 Category/index.vue 样式差异并重新构建发布，不覆盖工作区其他已有改动。
