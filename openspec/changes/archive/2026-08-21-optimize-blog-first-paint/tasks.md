## 1. 建立基线与自动检查

- [x] 1.1 记录当前线上首页 HTML、关键 JS/CSS、Banner 和接口的状态码、传输体积、压缩/缓存响应头及 3Mbps 冷缓存性能基线
- [x] 1.2 为博客生产构建启用 manifest，并实现初始 JS 700KB、初始 CSS 180KB、单 JS 分块 450KB 的 gzip 体积预算脚本
- [x] 1.3 将资源预算检查接入博客构建或部署前校验，并让超限结果输出具体资源后失败退出

## 2. 修复加载状态与首屏可见性

- [x] 2.1 重构 HTTP 请求跟踪，为每个请求记录加载元数据并以共享幂等 finalize 覆盖成功、失败、取消和超时路径
- [x] 2.2 修复并发请求计数与 body 滚动恢复逻辑，移除会误清空其他请求状态的全局 Map 清理
- [x] 2.3 为请求 finalize 增加成功、网络错误、超时、取消及交错并发的自动化测试
- [x] 2.4 将网站信息、Banner 和文章读取改为局部 pending/error 状态，使导航、首屏容器和降级文案无需等待接口即可显示
- [x] 2.5 为慢接口与失败接口补充首页视觉测试，确认不存在永久全屏遮罩且页面仍可导航、滚动

## 3. 缩减首页关键依赖

- [x] 3.1 移除会造成循环初始化的机械分包，保留路由/功能动态边界并由 Rollup 基于真实依赖图生成稳定分块
- [x] 3.2 检查所有公开路由保持动态导入，并让编辑器、图表、音乐播放器及非关键上下文菜单增强脱离首页初始依赖
- [x] 3.3 将反调试组件改为默认关闭、环境变量显式启用并在首屏稳定后动态加载
- [x] 3.4 将打包可视化报告改为仅在显式分析模式生成，完成构建后验证首页初始链路满足资源预算

## 4. 优化 Banner 关键图像

- [x] 4.1 为唯一 Banner 容器设置响应式尺寸占位，并为首图增加高优先级加载提示和适用时的 preload
- [x] 4.2 增加 Banner 图片失败降级背景和可读标题，验证桌面端与移动端无明显布局偏移
- [x] 4.3 校验 Banner 及带哈希图片的缓存头和实际传输体积，避免 base64 内联或重复请求进入关键路径

## 5. 配置生产压缩与缓存

- [x] 5.1 修复 Vite gzip 产物路径，确认每个符合阈值的 JS、CSS、SVG 和 JSON 旁存在可解压且内容一致的 `.gz` 文件
- [x] 5.2 更新项目中的 Nginx 部署配置，启用 `gzip_static`、动态 gzip 兜底、`Vary`、哈希资源长期缓存和入口 HTML 重新验证
- [x] 5.3 扩展部署脚本，在切换版本前验证 Nginx 配置，并在切换后检查首页、关键接口、压缩响应头、缓存头和资源体积
- [x] 5.4 保留上一版静态目录与 Nginx 配置，验证任一健康检查失败时可以明确报错并按流程回滚

## 6. 综合验收与部署

- [ ] 6.1 执行前端类型检查、单元测试、生产构建与资源预算检查，修复全部失败项
- [x] 6.2 在桌面端和移动端验证首页、文章、工作经历、相册和搜索等核心路由功能及视觉无回归
- [ ] 6.3 在冷缓存、移动视口和约 3Mbps 下行条件下重复测量，确认 FCP 不超过 2.5 秒、LCP 不超过 4 秒、CLS 不超过 0.1
- [x] 6.4 部署到服务器并运行线上验收，记录优化前后关键资源体积和首屏指标对比
