## 1. 路由基线与安全检查

- [x] 1.1 盘点前台与后台的静态路由、数据库动态菜单、组件跳转、面包屑、分享链接、TDK 匹配和部署层重写，形成旧路径到规范路径的完整对照
- [x] 1.2 增加路由审计校验，检查页面路径唯一性、路由名称唯一性、最多三级、无可选身份参数及无旧路径引用；发现无法满足三级限制的页面时暂停该页面并向用户报告
- [x] 1.3 记录前端 API 客户端路径基线，确保后续页面路由修改不触及后端 API 路径或契约

## 2. 前台规范路由

- [x] 2.1 建立前台规范路由名称与路径定义，并按设计路由表重写 Vue Router 记录，拆分列表/详情可选参数路由
- [x] 2.2 将“个人博客”、动态分类子导航、文章卡片、分类/标签/归档入口及首页按钮统一迁移到 `/blog` 路由体系
- [x] 2.3 迁移工作经历、留言、友链、相册、认证、账户等页面的所有组件跳转和链接到规范路径
- [x] 2.4 更新前台面包屑、搜索结果、分享链接、TDK 路由匹配与 canonical URL，使文章只使用 `/blog/articles/:id`
- [x] 2.5 将前台兜底路由改为渲染未找到页面，并确认旧路径不注册重定向或别名

## 3. 管理后台规范路由

- [x] 3.1 建立后台规范路由定义，调整静态/动态路由生成逻辑，并使 `/admin` 默认进入 `/admin/site/info`
- [x] 3.2 将文章列表、新建和编辑页迁移到 `/admin/articles`、`/admin/articles/new`、`/admin/articles/:id`，移除以 `?id=` 标识编辑页面的跳转方式
- [x] 3.3 迁移网站管理和系统管理下的页面跳转、标签页、面包屑及授权辅助页面到设计中的浅层规范路径
- [x] 3.4 保留后台真实未找到页面，确认旧后台路径不会被动态路由转换、静态别名或兼容重定向重新注册

## 4. 菜单数据迁移

- [x] 4.1 编写可重复执行且仅作用于已知菜单记录的 SQL 迁移，更新菜单 path/component 映射而不改变权限标识和业务数据结构
- [x] 4.2 同步初始化 SQL 与部署脚本中的菜单路由，并提供迁移前菜单路由备份/回滚 SQL
- [x] 4.3 验证菜单缓存刷新流程，确保部署后侧边栏只生成新路径且仍仅保留网站管理和系统管理分组

## 5. 验证与交付

- [x] 5.1 运行前后台类型检查与生产构建，并执行路由唯一性、三级深度、旧路径残留和 API 路径未变的自动校验
- [x] 5.2 在桌面端和移动端验证首页到博客、分类、标签、文章、认证及其他固定页面的导航与直接刷新
- [x] 5.3 验证后台默认落地页、菜单权限、文章新建/编辑、系统管理页面、直接刷新以及前后台旧路径的未找到行为
- [x] 5.4 按“菜单迁移 → 缓存刷新 → 后台资源 → 前台资源”的顺序部署，并完成线上规范路径、TDK、API 请求和 404 冒烟检查
