## 1. 分类 slug 基础能力

- [x] 1.1 实现共享分类 slug 工具，覆盖中文逐字拼音首字母、首个英文单词、混合名称、URL 规范化和保留字集合
- [x] 1.2 实现基于分类 ID 的确定性冲突消歧、slug 反向解析和统一分类路由构造函数
- [x] 1.3 为纯中文、纯英文、混合名称、重复结果、保留字、符号及无法转换名称补充单元测试

## 2. 规范路由迁移

- [x] 2.1 将前台分类规范路由改为 `/blog/:slug`，确保 `/blog/articles/:id`、`/blog/tags` 和 `/blog/archive` 等固定路由优先匹配
- [x] 2.2 删除 `/blog/categories`、`/blog/categories/:id` 对应路由与独立分类索引页面入口，不添加别名或兼容重定向
- [x] 2.3 更新页面 SEO 映射，使全部文章与分类筛选状态生成正确的标题、描述和 canonical URL

## 3. 聚合页内联筛选

- [x] 3.1 重构博客聚合页，使 `/blog` 与 `/blog/:slug` 始终复用刊头、分类导航和文章内容布局
- [x] 3.2 根据路由 slug 解析分类 ID并调用现有分类文章接口，同时保持“全部文章”返回 `/blog`
- [x] 3.3 在文章区域实现分类加载、空内容、错误重试和未知 slug 的 404 状态，不再渲染独立分类详情版式
- [x] 3.4 增加最新请求保护，验证快速切换分类时过期响应不会覆盖当前文章列表
- [x] 3.5 验证直接访问、刷新以及浏览器前进后退均能恢复 URL 对应的选中分类和文章内容

## 4. 全部分类入口统一

- [x] 4.1 更新聚合页分类按钮、桌面导航和移动导航，统一通过共享工具生成 `/blog/:slug`
- [x] 4.2 更新文章详情分类按钮、分类索引或上下文菜单等剩余入口，移除对 `/blog/categories/` 和原始分类 ID URL 的拼接
- [x] 4.3 全局审计分类链接生产者，确认每个分类只有一个页面路由且所有入口行为一致

## 5. 验证与交付

- [x] 5.1 扩展路由审计与相关测试，确认全部页面不超过三级、旧分类路由进入 404、文章静态路由不被动态 slug 捕获且 API 基线未变化
- [x] 5.2 运行前台单元测试、生产构建、性能预算和路由检查，修复所有本次变更引入的问题
- [x] 5.3 在桌面与手机尺寸验证筛选交互、选中状态、加载与空状态、长分类名称及无横向溢出
